# Agent 工作守则

## 项目信息
- **项目名称：** AI Learning（Duolingo 风格 AI 学习产品）
- **用户群体：** 中文用户
- **技术栈：** Next.js, MiniMax API, Tailwind CSS, Framer Motion, localStorage

---

## 开发规范

### 1. 界面文本规范

**核心原则：** 界面文本要贴合实际使用场景，不要硬翻单词含义。

**具体要求：**
- 面向中文用户，所有用户可见文本必须使用中文
- "Learning AI" 标题和首页 Logo 除外
- 翻译时要考虑用户真实使用场景，不是逐字翻译

**示例：**
| 场景 | ❌ 错误（硬翻） | ✅ 正确（自然） |
|------|----------------|----------------|
| 加载中 | 加载中... | 加载课程中... |
| 找不到页面 | 未找到课程 | 找不到这门课程 |
| 生成内容 | 正在生成内容... | 正在生成学习内容... |
| 完成提示 | Lesson Complete! | 太棒了，完成啦！ |
| 继续按钮 | 继续 → | 下一节 → |

### 2. Agent Team 协作规范

**触发条件：** 当有多个独立任务需要处理时，必须使用 agent team 分工。

**工作流程：**
1. 使用 `TodoWrite` 创建任务列表，明确每个任务的负责人
2. 使用 `Task` tool 启动 subagent，每个 subagent 负责任务的一部分
3. 定期同步进度，协调依赖关系
4. 最终合并所有工作成果

### 3. 代码质量规范

**提交规范：**
- 每次提交应该是一个完整的、可以独立工作的改动
- 提交信息使用中文，描述本次改动的目的
- 不要在提交中混入不相关的改动

**Review 规范：**
- 每次代码改动后需要自检
- 检查：变量命名、错误处理、边界情况、用户体验

### 4. 问题追踪规范

**CHANGELOG 维护：**
- 每次修复 bug 或新增功能，记录到 `CHANGELOG.md`
- 记录格式：日期、问题描述、解决方案、涉及文件

**常见问题归类：**
- **API 相关：** 检查端点、参数、响应格式
- **状态管理：** 检查 context、localStorage 同步
- **类型错误：** 检查 TypeScript 类型定义

---

## 技术注意事项

### MiniMax API
- **端点：** `https://api.minimaxi.com/v1/chat/completions`
- **模型：** `MiniMax-M2.7`
- **注意：** 答案校验时要考虑 AI 返回格式（如 "A" vs "A. 选项"）

### Tailwind CSS
- v4 版本使用 `@import "tailwindcss"` 而非 `@tailwind` 指令
- v4 不支持 `@apply` 指令，使用原生 CSS

### Next.js
- 路由：`/app/page.tsx` → `/`，`/app/api/xxx/route.ts` → `/api/xxx`
- 页面间导航使用 `useRouter`

---

## 待优化项（持续更新）

1. [ ] 学习卡片翻转动画体验优化
2. [ ] 错误处理的用户提示优化
3. [ ] 离线支持（无网络时显示已缓存内容）
4. [ ] 移动端适配优化
5. [ ] 答案校验逻辑（考虑 AI 返回格式差异）
